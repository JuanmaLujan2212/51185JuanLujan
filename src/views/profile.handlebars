<div>
    <h1>Tu perfil</h1>
    <p>{{user.first_name}}</p>
    <p>{{user.email}}</p>
    <p>{{user.age}}</p>
    <p>{{user.rol}}</p>

    <a href="/api/sessions/logout"><button>Salir</button></a>
</div>